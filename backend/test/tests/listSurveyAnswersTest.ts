

describe('List Survey answers Testes', () => {
  it('Should list a survey answers non ordered', function () {
    const allGood = true
    allGood.should.be.true()
  })
  it('Should list a survey answers ordered by asc', function () {
    const allGood = true
    allGood.should.be.true()
  })
  it('Should list a survey answers ordered by desc', function () {
    const allGood = true
    allGood.should.be.true()
  })
})
